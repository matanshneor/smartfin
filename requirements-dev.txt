-r requirements.txt
pytest>=7.4
# מדידת כיסוי. הפער שגילה אותה: מסלולי הכסף — הוספה, עריכה, מחיקה
# ואיפוס — לא הוזכרו בשום בדיקה, ומאות בדיקות רצו ירוקות מעליהם. בלי
# מספר אי אפשר היה לראות את זה, ובלי רצפה אי אפשר למנוע חזרה.
pytest-cov>=5.0
